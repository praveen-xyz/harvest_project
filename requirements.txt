# =============================================================================
#  Web Image Extractor — Python Dependencies
#  Install with:  pip install -r requirements.txt
# =============================================================================

# Web framework
flask>=3.0.0

# HTTP client (used for fetching pages and downloading images)
requests>=2.31.0

# HTML parser (used for extracting image URLs from pages)
beautifulsoup4>=4.12.0

# HTML parser backend for BeautifulSoup (faster than html.parser)
lxml>=5.0.0

# Production WSGI server (alternative to Flask's built-in dev server)
waitress>=3.0.0
